<% content_for :title, "Edit #{@affiliate.code} — Licencio" %>

<div class="mb-8">
  <%= link_to "← #{@affiliate.code}", admin_affiliate_path(@affiliate), class: "link-muted text-sm" %>
  <h1 class="mt-2 text-xl font-semibold tracking-tight">Edit affiliate</h1>
</div>

<div class="card max-w-xl p-6">
  <%= render "form", affiliate: @affiliate %>
</div>
