<% content_for :title, "Edit #{@product.name} — Licencio" %>

<h1 class="mb-6 text-xl font-semibold">Edit <%= @product.name %></h1>
<div class="max-w-xl"><%= render "form", product: @product %></div>
